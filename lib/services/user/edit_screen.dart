import 'package:flutter/material.dart';

class UserEditPage extends StatelessWidget {
  const UserEditPage({super.key, this.id});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
